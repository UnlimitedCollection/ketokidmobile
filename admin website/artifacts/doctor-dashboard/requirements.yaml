packages:
  "framer-motion": "For smooth page transitions and micro-interactions"
  "recharts": "For the dashboard phase distribution and kid weight charts"
  "date-fns": "For formatting dates cleanly in the UI"
  "clsx": "For conditional tailwind class merging"
  "tailwind-merge": "For merging tailwind classes without conflicts"
  "@hookform/resolvers": "Zod validation for react-hook-form"
  "react-hook-form": "For form state management"

images: []

notes:
  - Assumes standard session-based auth where /api/auth/me returns 401 if not logged in.
  - The API client react hooks from @workspace/api-client-react are generated and ready to use.
